{
  "description": "cc-speak: read aloud last assistant message after each task",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/run.py 2>/dev/null || python ${CLAUDE_PLUGIN_ROOT}/hooks/run.py",
            "timeout": 5000,
            "description": "cc-speak: TTS for last assistant message"
          }
        ]
      }
    ]
  }
}
